---
phase: 06
plan: 01
title: "gemini.ts → safeReaddir + session-start hotness SYNC guard"
wave: 1
depends_on: []
autonomous: true
requirements: [CODE-03, MEM-04]
files_modified:
  - src/agents/gemini.ts
  - tests/hotness-sync.test.mjs
estimated_tasks: 4
---

# Plan 06-01: gemini.ts → safeReaddir + session-start hotness SYNC guard

<objective>
Устранить дублирование кода: привести gemini.ts к использованию shared utils (safeReaddir) вместо прямых fs-вызовов, и добавить SYNC guard тест для hotness формул, дублированных в standalone session-start.ts. Вариант C (SYNC тест) выбран для session-start.ts, т.к. скрипт standalone и не может импортировать из dist/. Также обновить ROADMAP success criteria фазы 6 для соответствия реальности.
</objective>

<must_haves>
- gemini.ts использует safeReaddir из shared utils вместо прямого readdirSync
- session-start.ts hotness константы проверены SYNC тестом на идентичность с memory/hotness.ts
- ROADMAP Phase 6 Success Criteria обновлены: SC1 → safeReaddir (не safeReadJson), SC2 → SYNC guard (не импорт)
- Все существующие тесты проходят без регрессий
- npm run check (lint + typecheck) — 0 errors
</must_haves>

<deviation_log>
### DATA-01 не покрывается этим планом
**Причина:** Research показал что gemini.ts не читает JSON/JSONL напрямую — работает через git и readSessionIndex(). DATA-01 (JSONL парсер пропускает невалидные строки) уже покрыт в Phase 1 (parseJsonlFile в src/utils/jsonl.ts). Убран из requirements плана.

### ROADMAP SC1: safeReaddir вместо safeReadJson/parseJsonlFile
**Причина:** gemini.ts не использует JSON/JSONL парсинг напрямую. Реальный gap — прямой readdirSync без Result-обёртки. SC1 обновляется в задаче 06-01-04.

### ROADMAP SC2: SYNC guard вместо импорта
**Причина:** session-start.ts — standalone скрипт (копируется в ~/.claude/scripts/), не может импортировать из dist/. Вариант C (SYNC guard тест) — единственный безопасный подход. SC2 обновляется в задаче 06-01-04.
</deviation_log>

<tasks>

<task id="06-01-01" name="gemini.ts → shared utils (safeReaddir)">

**What:** Заменить прямой `readdirSync` в gemini.ts на `safeReaddir` из `src/utils/fs.ts`. Поскольку `safeReaddir` возвращает `Result<string[]>` (без `withFileTypes`), адаптировать код: получить список имён через `safeReaddir`, затем `statSync` для проверки isDirectory. Убрать прямой импорт `readdirSync` из `node:fs` если он больше не нужен.
**Why:** CODE-03 — адаптер должен использовать shared utils для тестируемости и единообразия. Shared utils обрабатывают ошибки gracefully (Result pattern), не падают.
**Files:** `src/agents/gemini.ts`

<verify>
<automated>npm run build && npm test -- --testPathPattern="resume-qwen-gemini" && grep -c "readdirSync" src/agents/gemini.ts | grep "^0$"</automated>
</verify>

</task>

<task id="06-01-02" name="SYNC guard тест для hotness констант">

**What:** Создать `tests/hotness-sync.test.mjs` — тест который импортирует `CATEGORY_WEIGHTS`, `DECAY_TAU_DAYS`, `calculateHotness` из `dist/memory/hotness.js` и проверяет что значения идентичны жёстко заданным значениям из `session-start.ts`: DECAY_TAU_DAYS === 60, все 6 категорий CATEGORY_WEIGHTS совпадают, формула calculateHotness даёт те же результаты на reference input.
**Why:** MEM-04 — memory loading при старте сессии использует hotness формулы; SYNC guard гарантирует что standalone копия в session-start.ts не разъедется с каноническим модулем.
**Files:** `tests/hotness-sync.test.mjs`

<verify>
<automated>npm test -- --testPathPattern="hotness-sync"</automated>
</verify>

</task>

<task id="06-01-03" name="Полная валидация: lint, typecheck, все тесты">

**What:** Запустить `npm run check` (lint + typecheck) и `npm test` — убедиться что 0 errors и все тесты зелёные. При необходимости исправить lint warnings в изменённых файлах.
**Why:** Quality gate — фаза не может считаться завершённой без полной проверки.
**Files:** `src/agents/gemini.ts`, `tests/hotness-sync.test.mjs`

<verify>
<automated>npm run check && npm test</automated>
</verify>

</task>

<task id="06-01-04" name="Обновить ROADMAP Phase 6 Success Criteria">

**What:** В `.planning/ROADMAP.md` обновить Phase 6 Success Criteria:
- SC1: `gemini.ts использует safeReadJson/parseJsonlFile из shared utils` → `gemini.ts использует safeReaddir из shared utils вместо прямого readdirSync`
- SC2: `session-start.ts импортирует hotness формулы из memory/hotness.ts, не дублирует` → `session-start.ts hotness константы проверены SYNC guard тестом на идентичность с memory/hotness.ts`
**Why:** Приведение ROADMAP в соответствие с реальностью: gemini.ts не читает JSON/JSONL, session-start.ts — standalone скрипт.
**Files:** `.planning/ROADMAP.md`

<verify>
<manual>Проверить что SC1 и SC2 в ROADMAP соответствуют must_haves плана</manual>
</verify>

</task>

</tasks>

<verification>
## Plan Verification

- [ ] gemini.ts использует safeReaddir из shared utils
- [ ] readdirSync убран из gemini.ts (grep подтверждает 0 вхождений)
- [ ] SYNC guard тест проверяет идентичность hotness констант
- [ ] ROADMAP SC1 и SC2 обновлены
- [ ] All must_haves met
- [ ] All tests pass (npm test)
- [ ] npm run check — 0 errors
- [ ] No regressions в существующих тестах
</verification>
